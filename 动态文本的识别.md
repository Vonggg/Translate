# 动态文本识别需求与实现方向

## 一、目标

当前项目既包含静态资源文本，也包含写死在 IL2CPP 代码中、运行时拼接、枚举转换、解密或动态加载后才显示的文本。

脚本 3“分析 IL2CPP 显示链路并生成动态 Hook C++ 字典”的目标是：尽可能准确地找出最终会显示到屏幕、但无法由静态资源翻译覆盖的文本，并生成 `native_unity_translation_dictionary.generated.cpp` 可直接使用的动态翻译字典。

核心要求是依据真实数据流或执行证据判断文本是否会显示，不能因为文本看起来像自然语言、以冒号结尾或像按钮标题，就直接加入字典。

## 二、脚本 3 的候选来源约束

### 1. `records.json`

`records.json` 只作为静态资源显示文本的参照集合：

- 先提取其中已识别的显示文本。
- 只在相同文本也存在于 `stringliteral.json` 时，直接确认对应 `stringliteral` 地址。
- `records.json` 中的文本不能单独加入动态字典。
- 不存在 `records.json` 时直接跳过该交集步骤。

### 2. `stringliteral.json`

除上述交集外，其余普通代码文本只能从 `stringliteral.json` 取得，并通过 IL2CPP 调用链证明它最终进入显示函数。

不得读取旧 C++ 字典、人工历史词条或其他项目结果并自动保留；每次结果必须由本次输入和本次分析重新得出。

### 3. 运行时枚举文本

`Steam`、`DocumentsFolder`、`cal12` 等名称不是 `stringliteral`，而是枚举成员经 `System.Enum.ToString()` 动态生成。这类文本允许作为独立来源，但必须同时满足：

- 从 `dump.cs` 和 IL2CPP 元数据确认真实枚举类型及成员。
- 追踪到该枚举值经过 `Enum.ToString()`。
- 转换结果最终进入已证明的显示函数。
- 只导出已经证明会显示的枚举类型，不能导出全部枚举。
- 枚举译文独立保存在 `records/runtime_enum_trans.json`。

## 三、最终显示入口

分析目标应覆盖项目实际支持的 Unity 显示体系：

- TextMeshPro：`TMP_Text.set_text`、`SetText`、`TextMeshProUGUI.GenerateTextMesh`、`TextMeshPro.GenerateTextMesh` 等。
- 普通 Unity UI：`UnityEngine.UI.Text.set_text`、`OnPopulateMesh` 等。
- NGUI：`UILabel` 的文本设置及最终文本刷新/生成入口。
- 已确认具有显示语义的项目包装函数。

判断调用链时，最终渲染总阀通常比单纯的 `set_text` 覆盖更广。不能假设所有文本都必然经过直接 setter；有些文本可能先写入字段，再由 Unity 生命周期或渲染函数读取。

## 四、目前已经支持的静态链路

脚本 3 当前已经逐步支持：

- `set_text("字符串")` 等直接调用。
- 局部变量转发，例如 `a = "字符串"; set_text(a)`。
- `String.Concat`、`String.Format`、数值 `ToString()` 等拼接和格式化。
- 字符串返回函数及多层包装函数。
- 对象字段、嵌套字段、静态字段的写入和后续读取。
- `Action<string>`、`Action<int, string>` 和其他标准委托订阅、回调与调用链。
- 返回自定义 UI 对象的工厂函数，以及该对象中的 Text/TMP 字段。
- 泛型共享后被擦除为 `object` 的 `GetComponent<T>`，通过 `ScriptMetadataMethod` 恢复真实组件类型。
- `Dictionary`/`List` 中字符串的写入、跨方法传递、枚举读取和最终显示。
- 枚举装箱、`Enum.ToString()`、后续拼接以及显示链路。

例如 `Money: 2000` 的实际链路为：

```text
"Money: " + Money.ToString()
→ Dictionary<string, Sprite>
→ DailyBonusesManager.ShowBonusReward
→ 遍历 Dictionary
→ GetComponent<BonusInfoPrimitive>()
→ BonusInfoPrimitive.InfoText.text
```

字典只需要保存可稳定匹配的动态片段：

```cpp
{u"Money: ", u"金钱: "},
```

运行时数值继续由游戏生成，最终得到 `金钱: 2000`。

## 五、目前仍然可能遗漏的情况

纯静态 ARM64 抽象解释仍可能在以下位置断链：

- 多层未知虚调用、接口调用和复杂泛型共享。
- 自定义事件系统、消息总线、反射或运行时注册的回调。
- 复杂容器、闭包、协程状态机及跨线程队列。
- 依赖场景对象、存档状态、网络结果或用户操作的条件分支。
- 运行时解密、解压或动态资源加载后才产生的文本。
- 多段动态拼接中不存在完整 `stringliteral` 的最终字符串。
- 同一函数中不同分支将字符串用于显示、日志或逻辑标识，静态状态合并后无法准确区分。

不能通过“短文本”“自然语言”“冒号结尾”等文本外观规则掩盖链路解析问题。无法证明时应保留明确的断链原因，供后续分析。

## 六、使用 unidbg 进行显示链路定向 Fuzzing

### 1. 定位

unidbg 不用于完整模拟整个 Unity 游戏，而作为脚本 3 静态分析后的第二级补漏器，只处理 `not_proven_display` 等尚未证明的候选。

整体流程：

```text
未证明的 stringliteral
→ 找到引用该地址的起始函数
→ unidbg 加载对应 ARM64 libil2cpp.so
→ 构造 this、参数和最小对象内存
→ 给源字符串附加污点标记
→ 定向执行并探索不同条件分支
→ 追踪寄存器、栈、字段、容器及返回值
→ 污点到达 Text/TMP/NGUI 显示入口
→ 标记为动态显示文本
```

这不是普通随机 Fuzzing，而是“显示目标引导的覆盖 Fuzzing + 字符串污点追踪”。

### 2. 路径探索

- 从字符串的直接引用函数开始，不从游戏入口启动。
- 根据比较指令生成边界值，如 `0`、`1`、`-1`、数组长度和枚举范围。
- 优先翻转尚未覆盖的条件跳转。
- 对布尔值、整数、枚举、空指针和对象字段进行定向变异。
- 保存产生新基本块覆盖率或新间接调用目标的输入。
- 使用上下文快照或重新执行探索多个分支。
- 命中显示入口后立即结束该字符串的探索。
- 设置单字符串时间、指令数、分支数、递归深度和内存上限，避免路径爆炸或长时间无输出。

一次或多次未命中只能表示“在当前预算内未证明”，不能据此判定文本绝不会显示。

### 3. 自动对象构造

- 使用 `dump.cs` 的类型、字段偏移和方法签名生成最小 `this` 对象与参数。
- 使用 `script.json`、`ScriptMetadataMethod` 和重定位表恢复泛型实例及 MethodInfo。
- 空指针导致路径提前结束时，自动尝试创建对应字段对象。
- UI 组件只需构造能支持文本字段和虚表调用的最小桩，不需要真实渲染。
- 对明显属于游戏状态的字段生成小规模候选值，而不是填充完全随机数据。

### 4. 字符串污点传播

每个源 `stringliteral` 使用独立污点 ID。至少需要为以下操作提供摘要或 Hook：

- `String.Concat`
- `String.Format`
- `StringBuilder` 系列
- 数值、枚举和对象的 `ToString()`
- 字符串截取、替换及大小写转换
- 数组、`List`、`Dictionary` 的写入和读取
- 字段写入、字段读取、函数返回值
- 委托参数、事件调用及虚函数参数

外部函数不必全部真实执行：与文本无关的函数可返回受控桩值；文本变换函数必须保留污点；显示函数作为目标点直接截获。

### 5. 命中标准和证据

只有污点文本实际到达可信显示入口时，才视为 unidbg 已证明。每次命中至少记录：

- 原始字符串及 `stringliteral` 地址。
- 起始方法、调用点和目标显示函数。
- 实际间接调用/虚调用目标。
- 经历的字符串变换、字段和容器。
- 触发命中的输入及对象字段值。
- 基本块覆盖信息和执行耗时。
- 最终是整句匹配还是任意位置替换。

推荐在 `stringliteral_display_analysis.json` 中增加独立的 `emulated_display_literals` 区域，不要把模拟命中伪装成静态分析结果。

## 七、结果分级

建议按证据强度区分：

1. `static_proven_display`：静态调用链已经完整证明。
2. `emulated_proven_display`：unidbg 执行时污点到达显示入口。
3. `runtime_observed_display`：真机运行时在最终渲染入口实际观察到。
4. `not_proven_display`：在当前分析与执行预算内仍未证明。

前三类都可以进入翻译候选，但报告中必须保留来源类型。启发式文本外观只能决定分析优先级，不能提升为“已证明”。

## 八、动态字典生成

- 原文原样进入翻译缓存，不把运行时数字硬编码进源文本。
- 直接作为完整文本显示的候选进入整句匹配字典。
- 经过拼接、格式化、枚举转换或容器组合的稳定片段进入任意位置替换字典。
- `{0}` 等格式化占位符必须原样保存。
- 多条规则可能相互包含时，优先输出更长的键，避免短键提前破坏长键匹配。
- 生成结果仍写入：

```text
workspace项目名/output/Hook_Translate/native_unity_translation_dictionary.generated.cpp
```

- 脚本 4 重建 `game.txt` 和 `game_chars.txt` 时，继续同时合并 `trans.json` 与最终动态 C++ 字典中的全部原文和译文字符。

## 九、缓存和运行要求

- 该分析脚本相对独立，通常每个项目只运行一次。
- 每次调用前清理本脚本自己的分析缓存，不能因旧缓存掩盖逻辑更新或项目变化。
- 可以保留已经完成的翻译缓存用于断点续跑，但旧缓存中不属于本次候选的键不得成为隐式候选来源。
- 执行过程中必须定期输出阶段、候选数、覆盖进度、当前字符串和剩余预算，不能长时间没有任何信息。
- unidbg 补漏只处理静态未证明候选，并设置严格资源预算，避免重新模拟已经由静态链路确认的内容。
- 模拟异常、缺失依赖或预算耗尽必须记录具体原因，不得让整个脚本无限等待。

## 十、真机采集作为最终补充

unidbg 的一次执行只覆盖具体输入对应的路径，依赖真实场景、网络、资源和 Unity 生命周期的文本仍可能无法模拟。

后续可在现有 `native_unity_api.cpp` 中加入只记录、不翻译的运行时采集模式，在 TMP、普通 UI 和 NGUI 的最终渲染入口记录实际显示过的原文。真机采集不替代静态分析和 unidbg，而作为最终证据与遗漏反馈来源。

最终推荐链路：

```text
records.json ∩ stringliteral.json 直接确认
→ IL2CPP 静态显示链路分析
→ unidbg 显示目标定向 Fuzzing 补漏
→ 真机最终渲染入口采集验证
→ AI 翻译
→ 生成动态 Hook C++ 字典
```

## 十一、验收标准

- `Money: 2000` 这类“固定前缀 + 动态数字”能够识别固定前缀并生成任意位置替换。
- `Steam`、`DocumentsFolder`、`cal12` 等经 `Enum.ToString()` 生成且确实显示的枚举成员能够被识别。
- 仅用于 Analytics、日志、ID、路径、序列化或游戏逻辑的相同文本不能因为外观相似而自动加入字典。
- 对委托、字段、容器、泛型组件和虚调用造成的断链，应优先修复通用数据流，不增加单项目文本白名单。
- 每个词条均可追溯到 records 交集、静态链路、模拟命中或真机观察中的一种明确证据。
- 脚本重复运行结果稳定，不依赖旧 C++ 字典或人工历史词条。
